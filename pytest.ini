[pytest]
asyncio_mode = auto
pythonpath = src
markers =
    unit: Fast tests without external network calls (default marker for cheap tests).
    integration: Tests that call real external services (LLM, RAG, LMS).
    expensive: Costly tests that consume paid API tokens or significant time.
